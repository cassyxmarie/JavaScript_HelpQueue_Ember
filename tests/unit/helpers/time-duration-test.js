import { timeDuration } from 'help-queue/helpers/time-duration';
import { module, test } from 'qunit';

module('Unit | Helper | time duration');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeDuration([42]);
  assert.ok(result);
});
